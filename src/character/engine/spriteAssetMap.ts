/**
 * Production PNG base sprites are available under
 * `public/sprites/avatar/base/`. CharacterView now prefers the real pixel
 * renderer and safely falls back to ChibiFallbackArt if a required gender
 * or frame asset is unavailable.
 */
export const SPRITE_ASSETS_AVAILABLE = true
